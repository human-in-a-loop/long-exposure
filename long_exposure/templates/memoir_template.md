# Run memoir

<!-- Advisory narrative back-reference (created {created}).
     Where this disagrees with plan_of_record.md or the promise ledger,
     THEY win; fix this file. Superseded versions: memoir/history/ and
     search_sessions (record_type: memoir). Keep every section within
     its cap; the harness truncates at the global cap. Do not put next
     steps, verdicts, milestones, claims, evidence, or tool quirks here —
     each already has a home. -->

## Thesis
<!-- ≤ 3 sentences. The run's current best answer or frame — the
     interpretive stance everything else serves. Not a milestone, not a
     claim with evidence. -->
(none yet)

## Standing on
<!-- ≤ 5 items. Premises the run is PROCEEDING AS IF true but has not
     verified. Verified → move to the ledger and delete here.
     Broken → move to Ruled out.
     - <premise> — since cycle N — would change: <what breaks if wrong> -->
(none yet)

## Ruled out
<!-- ≤ 8 items. Research approaches tried and abandoned. NOT infra or
     tool failures — those are lemmas.
     - <approach> — <why it failed> — cycle N — see <report or path> -->
(none yet)

## Parked
<!-- ≤ 5 items. Deliberately deferred, with the trigger that reopens it.
     Committed work belongs in plan_of_record.md, not here.
     - <what> — reopen when: <trigger> — parked cycle N -->
(none yet)

## Where to look
<!-- ≤ 6 pointers. The L2 reports, files and session ids that carry the
     current thesis.
     - <path or session id> — <what it holds> -->
(none yet)

## Changed this cycle
<!-- 1–3 lines, REPLACED every cycle, never accumulates. What moved
     since the last memoir. -->
(first cycle — nothing yet)
