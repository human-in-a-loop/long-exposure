# Deferred Gaps

| Gap | Impact | Justification | Status |
|---|---:|---|---|
| `mcp_search_server.py` used LSP-style `Content-Length` framing, causing every `claude -p` call to hang for the 30 s MCP-connection timeout. | High | Discovered 2026-05-15 when an auditor cycle exited with code 1 and every prior cycle's `mcp-logs-sessions` JSONL showed `MCP server "sessions" connection timed out after 30000ms`. Direct stdio probe confirmed the server expected `Content-Length:` headers, while Claude Code 2.1.143 sends newline-delimited JSON per the current MCP stdio spec (revision 2024-11-05+). Fixed the server to read/write one JSON message per line; end-to-end `claude -p --mcp-config` now reports `Successfully connected (transport: stdio) in ~200 ms`. **Cross-provider impact:** Gemini CLI (wired through `.gemini/settings.json` and the same MCP server) would have hit the identical hang and is implicitly addressed by the same fix — no Gemini-side change required. Codex CLI is unaffected today because `Codex MCP config flag support` (separate gap row below) is still deferred, so no Codex path currently launches this MCP server. | Resolved |
| Periodic/merge reports captured the reporter's trailing cover-note instead of the report body, producing a ~3 KB stub `report_cycles_*.md` (YAML + `<checkpoint>` + "the report is in the `[OUTPUT: report]` block") rather than the full report. | High | Discovered 2026-05-28 (cycles 1–3 report). Two layered causes. **(1) Capture scope:** the periodic-report path parses `envelope["result"]` (`exploration.py:1404`/`:1420`), which is only the agent's *final* assistant message; the reporter emitted the 29 KB report in assistant block 8 of 10 and ended with a trailing checkpoint/cover-note (block 10), so the deliverable was never in `result`. With `require_checkpoint_first: false` agents freely trail content after the deliverable, so this recurs. **(2) Close-tag regex:** `_OUTPUT_RE` (`conductor.py:322`) matched only the bare `[END OUTPUT]`, never the named `[END OUTPUT: <name>]` agents actually emit — a latent Tier-1 miss affecting all roles (researcher/auditor only "work" because their Tier-2 fallback forwards the whole message downstream). **Fix applied locally:** generalized `_OUTPUT_RE` to accept `[END OUTPUT]` and `[END OUTPUT: <name>]`; made `parse_outputs` keep the longest block per name (inline mentions/stubs can't clobber the real block); added `_session_transcript_text()` + a guarded recovery in the agent parse path that re-parses the full session JSONL when `result` lacks a complete block. Verified end-to-end: recovers the real 29 439-char report from the incident session. New regression test `tests/test_output_block_recovery.py` (6 cases). **Cross-provider impact:** recovery resolves the Claude session-JSONL path; Codex/Gemini transcripts differ, so for those providers the regex + prefer-longest still help but transcript recovery is a no-op (returns "") — acceptable, and a follow-up can add their transcript shapes. | Resolved locally — batch into deferred PR (alongside the `mcp_search_server.py` framing fix); not yet pushed |
| The curator's `CURATION.yaml` was discarded and the run shipped the report-only "safety fallback" package even though the curator agent succeeded. | High | Discovered 2026-05-29 (daily-sync at T0902; curator logged `ok` with 20.6 KB output, then `WARNING: CURATION.yaml was not produced … safety fallback`). **Two coupled bugs.** **(1) Path mismatch:** the packager reads the manifest only from `working_dir/CURATION.yaml` (`curator.py`), but when the project lives in a subdirectory the curator writes it relative to its effective cwd — here `workspace/project-subdir/CURATION.yaml`. The valid manifest (correct `package_name`, workspace-relative `src`) was on disk but in the wrong directory, so the packager saw "not produced" and discarded it. **(2) Over-broad hard-exclude:** `_PACKAGE_HARD_EXCLUDE_PATTERNS` excluded the whole `reports/final/` subtree, which holds BOTH the canonical deliverables (`final_report.md`/`.pdf`) and the scratch drafts (`outline.md`, `draft.md`, `run_mode.json`, `.committed`). Any agent-authored manifest therefore had its primary report entry silently dropped at parse time — so even a correctly-placed manifest produced a package missing the final report. **Fix applied locally:** added `_locate_curation_file()` (shallow, depth-bounded, prefers canonical then shallowest recovered copy; ignores `report/` audit-trail copies and `.package_staging_*`) and a `curation_base` second resolution root in the staging loop so a misplaced manifest is recovered and its `src` entries resolve against either the workspace root or the manifest's own directory; narrowed the `reports/final/` exclude to the four scratch basenames only. Pinned the curator prompt (`exploration-score.yaml`) to write `{working_dir}/CURATION.yaml` (absolute) so misplacement does not recur. Verified end-to-end against the real incident artifacts: recovers `project-subdir/CURATION.yaml`, `fallback_used: False`, `final_report.md` now ships, correct package name. New regression test `tests/test_curator_recovery.py` (9 cases). **Remaining (secondary, separate):** the recovered manifest is still `curation_complete: false` because `MANIFEST.md` had no `## Key Files` section — the daily-sync runs the curator without a preceding final-reporter stage that authors it (see row below). | Resolved locally — batch into deferred PR; not yet pushed |
| Daily-sync curation can still be `curation_complete: false` when `MANIFEST.md` lacks a `## Key Files` section. | Medium | 2026-06-12 re-audit corrected the original premise: `_run_daily_sync` DOES run the final reporter between auditor and curator, and the reporter's staged protocol authors `## Key Files`. The residual windows where the section is absent are narrower: (i) first sync before any `report_cycles_*.md` exists (`_run_final_reporter` early-returns), (ii) the reporter fails or rate-limits (sync continues to curator by design), (iii) wall-cap/stop truncation. In those windows the curator's refusal to guess and the report-only safety package ARE the intended graceful degradation — option (b) (curator derives Key Files itself) would create a second author for a single-author contract and convert honest degradation into confidently-wrong inventory. Decision: keep the final reporter as sole Key-Files author; if a live sync shows case (ii) recurring, the fix is surfacing/retrying the reporter failure. | Re-assessed; degradation accepted by design |
| `_invoke_claude` only logs `result.stderr[:500]` on non-zero exit, so transient Claude API socket drops surface as `Claude CLI exited with code 1: <blank>` with no diagnostic. | Medium | When Anthropic's API closes a streaming connection mid-turn, the CLI writes the API error to the assistant content, routes it through the stdout JSON envelope, and exits non-zero — leaving stderr empty. **Resolved:** commit `3a1e481` added `_format_cli_failure_context` (orchestrator.py): all three providers' non-zero-exit branches now emit `stderr=…; result=…; stdout=…` (each capped at 500 chars, raw-stdout fallback when JSON parsing failed; Codex envelopes are normalized to the `result` key, so the cross-provider concern is moot). The one residual diagnostic-loss path — hard/idle timeouts deleting the stdout/stderr temp files unread — was closed in the 2026-06-12 audit sweep: `TimeoutExpired` errors now carry bounded 500-char tails of both streams. | Resolved |
| Compatibility names still say `ClaudeCliError`, `ClaudeRateLimitError`, and `call_claude`. | Medium | Renaming would touch many call sites and docs. Provider behavior can be generalized underneath without disrupting the architecture. | Deferred |
| Codex MCP config flag support. | Medium | Local `codex exec --help` did not expose a direct `--mcp-config` flag. Adding unverified flags would make Codex runs fragile. | Deferred |
| Codex reasoning effort flag mapping. | Low | ~~Local `codex exec --help` does not document a stable effort flag.~~ **Resolved** by the per-agent LLM routing change: Codex reasoning effort is now injected as `-c model_reasoning_effort="<level>"` (the config key is recognized — verified against the installed CLI under `--strict-config`). The canonical `low\|medium\|high\|xhigh\|max` vocabulary maps to Codex's `low\|medium\|high\|xhigh`, with `max` capped to `xhigh` (`max`/`ultra` are gpt-5.6-only and entitlement-gated, absent from the official config reference). Applied at every Codex command site (fresh/resume in `_call_exploration_agent`, compaction, and `call_claude`). Effort also still conditions the system prompt. | Resolved |
| Gemini per-agent effort is best-effort via `settings.json`, not a first-class CLI flag. | Low | Gemini CLI exposes **no** `--effort`/`--thinking-budget` flag (confirmed against CLI help and the upstream feature request google-gemini/gemini-cli#25122). Per-agent effort is applied by writing `model.generateContentConfig.thinkingConfig.thinkingLevel` (LOW\|MEDIUM\|HIGH for Gemini 3) into the generated `.gemini/settings.json`. Two residual weaknesses: (1) the exact wrapper keys vary across CLI versions, so a version that names them differently silently ignores the level (never errors — the write is additive and guarded); (2) Gemini 2.5 uses a numeric `thinkingBudget` rather than `thinkingLevel` and is not mapped (default model is `gemini-3-*`). Effort always also conditions the system prompt, so the role's intended effort is never fully lost. Revisit if a Gemini-3 CLI version stabilizes a documented settings key or ships the requested `/think` flag. | Deferred (best-effort) |
| Local (OpenAI-compatible) provider carries effort in the system prompt only. | Low | The `call_local_llm` payload has no `reasoning_effort` field; per-agent effort for `local` is advisory (system-prompt text) rather than an API parameter. `local` is documented as an unsupported extension point, and OpenAI-compatible servers vary in whether/how they accept a reasoning field, so injecting one blindly risks 400s. Add an opt-in `local_reasoning_effort` passthrough only if a concrete self-hosted target needs it. | Deferred |
| Minor per-agent-routing residuals (cosmetic / operator-error). | Low | (1) **Telemetry labels for out-of-cycle agents:** in per-agent-pinned mode the main cycle agents (researcher/worker/auditor) emit their real provider/model, but out-of-cycle agents (reporter/final_auditor/final_reporter/curator/manager/merge) still label telemetry events with the base `llm_provider`/`model` — the *actual call* runs on the correct pinned provider (correctness is unaffected); only the analytics label lags. (2) **Silent provider normalization:** an unknown/typo'd `provider:` value normalizes to `claude` (via `normalize_provider`) without a warning, unlike effort (which warns). (3) **Model↔provider mismatch:** a model that doesn't belong to the chosen provider (e.g. `{provider: codex, model: opus}`) is passed through and fails at that agent's CLI call (surfaced as an agent failure, not a crash); model validity is the operator's responsibility, guided by the config-reference notes. Fix the telemetry labels opportunistically by threading the agent's resolved provider/model into the out-of-cycle emit sites. | Deferred |
| Heterogeneous per-agent providers and multi-account pooling are mutually exclusive. | Low | When agents resolve to more than one distinct provider ("per-agent-pinned" mode), the run pins each agent to its provider and **defers** multi-account pooling (both the single-provider pool and the unified cross-provider pool), mirroring the existing interactive-transport deferral. This is the robust resolution of a real conflict: the unified pool's model is one active provider per root process with capacity/rate-limit rotation, which cannot also honor a fixed per-agent provider assignment. Homogeneous runs (including the default all-Claude template) keep pooling fully intact. Combining heterogeneous per-agent providers with simultaneous per-provider account rotation would require per-agent slot acquisition across provider pools — deferred as a low-value advanced combination. See docs/multi-account-pool.md. | Accepted trade-off |
| Codex temp-directory trust behavior. | Low | Codex rejected an untrusted temp workspace during smoke testing. Real long-exposure runs already use an explicit `working_directory`; operators should trust that directory through Codex. | Deferred |
| README and all operator docs fully rewritten as provider-neutral. | Low | Critical setup docs will be updated after implementation; deep wording cleanup is lower value than behavior verification. | Deferred |
| Generic local connector does not have provider-native conversation sessions. | Low | The local connector is an unsupported OpenAI-compatible extension point. Long-exposure can maintain per-agent JSONL transcript logs, bounded recent-log injection, and local compaction into `sessions.db`, but backend-native session semantics depend on the operator's server. | Known limitation |
| Local provider has no native subagent/team equivalent. | Medium | Implementing a custom local subagent scheduler would duplicate long-exposure fan-out and add fragility. Whole-cycle fan-out remains available; local intra-turn subagents are deferred. | Deferred |
| Gemini free-tier native subagents are not load-bearing. | Medium | Gemini native subagents are explicitly disabled in long-exposure. Whole-cycle fan-out remains available and was live-tested with concurrent Gemini sessions. | Known limitation |
| Gemini MCP session-search wiring is project-settings based. | Medium | Long-exposure writes `.gemini/settings.json` with the `sessions` MCP server and current tool allowlist. Gemini's `mcp list` command did not reliably display project settings in a smoke test, but normal Gemini tool execution under the generated settings worked. | Partially addressed |
| Gemini tool allowlist translation is coarse. | Medium | Gemini receives `tools.core`, `tools.allowed`, and `--allowed-tools` mapped from long-exposure `allowed_tools`; command-prefix Bash mappings are supported. Exact Claude-style path scoping still relies on `cwd`, prompt guidance, and external sandboxing. | Partially addressed |
| Gemini multi-account pooling is disabled. | Medium | Long-exposure intentionally ignores `GEMINI_ACCOUNT_POOL` and `GEMINI_HOMES` for Gemini. Fan-out works through concurrent sessions on the active Gemini CLI account; account rotation needs separate OAuth validation before it should be supported. | Known limitation |
| Self-hosted open-model deployment on Google Cloud is not cost-competitive for this project. | High | Gemini Flash on the free-tier path is the cheapest practical Google-hosted option. Self-hosted open models remain possible through the generic local connector, but are not a native long-exposure integration path. | Known limitation |
| Generic local connector has no executable tool bridge. | High | OpenAI-compatible chat completions can read tool guidance, but cannot call long-exposure tools without a separate tool-calling loop and sandbox policy. | Known limitation |
| Report formatter does not rewrite fragmented pseudo-tables into Markdown tables. | Low | Automatically repairing PDF-extracted or hand-wrapped pseudo-tables risks changing data. The renderer wraps verbatim blocks reliably; semantic table reconstruction is a human/editorial task when needed. | Deferred |
| Report formatter does not auto-generate explicit heading IDs for exotic headings. | Low | Normal heading spacing now prevents the common Pandoc TOC failure. Auto-slugging every heading could create noisy diffs and accidental duplicate IDs; explicit IDs remain available for rare collisions. | Deferred |
| Report render validation does not automatically raster-preview representative PDF pages in production runs. | Low | The core pipeline now uses deterministic settings and render failures are surfaced. Automatic `pdftoppm` previews add optional system-tool coupling and file churn; manual preview remains a troubleshooting step. | Deferred |
| Manager drift detection is mostly agentic rather than a deterministic topic classifier. | Low | The poller passes recent reports and directive context to the manager agent when action is already needed, but it does not maintain a bespoke keyword ontology per campaign. A generic deterministic drift classifier would be noisy without campaign-specific labels. | Deferred |
| Manager agent sessions are not persisted back into exploration_state.json. | Low | Avoiding state writes from the cron sidecar prevents races with the main conductor. The manager receives deterministic snapshots each poll, so persistent conversational memory is a convenience rather than required state. | Deferred |
| Main cycle does not alert on missed cron manager polls by schedule. | Low | The manager writes an assessment log every poll, but the conductor does not know the operator's cron cadence and therefore cannot distinguish intentional disablement from missed polls without extra configuration. | Deferred |
| Manager no-op polls still append `_manager/no-action` ledger events. | Low | This preserves a structured audit trail for every cron poll, but very frequent cron schedules can add ledger noise over multi-week runs. Assessment logs carry the detailed record; ledger compaction or no-op sampling can be added later if real runs show bloat. | Deferred |
| Setup does not run an LLM agent to repair environment failures automatically. | Low | First-run setup happens before provider credentials are reliably configured, so deterministic installer + JSON doctor output is the robust MVP. The JSON report is designed to be handed to an agent or issue tracker for debugging. | Deferred |
| Tectonic fallback installers beyond native package managers are not bundled. | Low | `long-exposure-setup` supports apt/dnf/yum/pacman/zypper/brew/winget and reports exact missing tools if a platform repository lacks `tectonic`. Pulling installers from cargo or upstream release URLs would add network/toolchain variability; revisit only if package-manager coverage proves insufficient. | Deferred |
| CLI adapter installer is template-based, not version-introspective. | Medium | `long-exposure cli-install` writes deterministic Claude/Codex/Gemini routing files with overwrite safeguards, but it does not inspect every installed CLI version to prove that each hook format is auto-loaded. The files route to the robust launcher and are safe; deeper version-specific adapter discovery can be added once real CLI version drift is observed. | Deferred |
| `long-exposure launch --manager` does not force an immediate manager poll at process start. | Low | On a fresh launch the state file may not exist until the conductor initializes. The embedded manager loop waits for its configured interval and cron/headless polling still works. Forcing an early poll would mostly produce no-state noise. | Deferred |
| Launcher tailing is intentionally minimal. | Low | Foreground `long-exposure launch` streams conductor output directly and `long-exposure status` surfaces the latest manager notice. A richer multiplexed tail of run logs plus manager JSONL could improve ergonomics, but it is not necessary for reliability and would add more terminal-state handling. | Deferred |
| Live Claude/Codex/Gemini smoke tests are still opt-in/manual. | Medium | The deterministic suite now covers provider command construction, Codex/Gemini parsing, one-cycle loop behavior, resume, and fan-out without credentials. Live smoke tests require user accounts and can be flaky under rate limits, so they should remain explicitly gated rather than part of normal tests. | Deferred |
| Telemetry does not parse provider-native inner tool transcripts. | Medium | The passive telemetry layer records Python-visible events, provider usage envelopes, report outcomes, fan-out outcomes, manager verdicts, and deterministic rollups. Parsing Claude/Codex/Gemini internal tool transcripts is provider/version-specific and should be added only after stable transcript fixtures exist. | Deferred |
| Telemetry context-ratio metrics are advisory, not a hard scheduler. | Low | Agent events now record estimated context tokens and the highest observed context ratio. Exceeding a provider context limit can still happen in live runs, especially when provider usage envelopes differ, but this is acceptable as a review signal and avoids adding fragile preemptive routing. | Deferred |
| Codex cached-token semantics are treated conservatively in context ratio. | Low | Codex reports `cached_input_tokens` separately from `input_tokens`; telemetry rollups count it for usage visibility but do not add it to context ratio unless a provider exposes explicit `cache_read_input_tokens`. This avoids inflated ratios, but still leaves provider-specific semantics approximate. | Deferred |
| Structural context-budget enforcement is deferred. | Medium | Live runs can still exceed provider context limits when prior reports, workspace inventory, and provider-specific cache semantics combine badly. A robust fix likely needs structural prompt budgeting, workspace-copy pruning, and per-role input caps rather than more prompt guidance. | Deferred |
| Telemetry lessons are deterministic scaffolds, not agent-authored analyses. | Low | `telemetry summarize` writes a lessons shell with evidence prompts. Running an analyst agent over telemetry is useful later, but keeping it out of the live path preserves passive behavior and avoids hallucinated causal claims during runs. | Deferred |
| Telemetry export is local-only. | Low | The MVP intentionally avoids network upload and sharing flows. A future redacted export bundle can be added after operators validate which fields are useful and safe to share. | Deferred |
| Telemetry path redaction defaults to off for local analysis. | Low | Prompt/response/tool/env fields are centrally omitted by default and account path keys are hashed, but normal artifact paths remain visible unless `redact_paths: true` is set. This preserves local debugging usefulness; a future share/export command should force path redaction. | Deferred |
| No automatic migration for old root-level final-stage artifacts. | Low | New runs route final report artifacts to `reports/final/` and final audit artifacts to `audits/final/`, and `org_check` notes legacy root artifacts without failing. Moving historical files automatically could surprise operators; a one-shot migration helper is only worth adding if real workspaces accumulate confusing legacy clutter. | Deferred |
| Delta mode does not resume inside a partially completed final-auditor/final-reporter stage chain. | Medium | The run writes `run_mode.json` and reuses deterministic scratch paths, but a killed delta pass restarts the staged pass instead of continuing at the next missing stage. Restarting is safe and simpler; stage-level resume can be added later if long delta passes make the wasted work material. | Deferred |
| Legacy-size delta detection cannot compute a precise new-artifact boundary. | Low | Explicit `*.committed` markers are used for new successful outputs and provide a timestamp boundary. Existing pre-marker workspaces fall back to the old size heuristic for compatibility, which may over-budget one continuation but avoids silently treating a prior final report as absent. | Deferred |
| Unified provider pool does not preserve per-provider native session IDs. | Medium | The first unified-pool implementation safely clears `agent_sessions` on cross-provider rotation instead of persisting a provider-keyed session map. This avoids passing a Claude session UUID to Codex/Gemini and preserves correctness through compaction summaries and workspace artifacts. Provider-sliced session continuity can be added later if live unified-pool runs show the extra continuity is worth the added state complexity. | Deferred |
| Branchial-budget fan-out log records fork id but not exact root cycle. | Low | Branch novelty annotations are attached before the fan-out conductor assigns fork metadata, and the conductor currently does not receive the root cycle. The manifest and JSONL sidecar still make the signal useful for review; threading cycle through the conductor is straightforward but not worth widening the call surface until reports need exact cycle grouping. | Deferred |
| Ledger graph summary does not infer bridge events across independent milestone chains. | Low | Final-stage summaries now expose chains, contradictions, and evidence density from the promise ledger. Inferring true bridge events requires a more opinionated graph algorithm and could create misleading causal claims, so the initial implementation stays read-only and conservative. | Deferred |
| Unified provider pool planned daily rotation remains provider-local. | Low | Rate-limit rotation now marks the exhausted unified root holder cooling, releases its slot, and acquires a fresh slot across configured providers. The lower-priority planned daily rotation still uses the legacy per-provider pool path, so it may rotate within the active provider instead of spreading usage across providers. This is not a correctness issue; cross-provider planned rotation can be added after live unified-pool telemetry shows it is useful. | Deferred |
| Graceful-preemption has no hard-kill escalation; a root killed with SIGKILL orphans live clones (no PPID/parent-liveness backstop in clones); `_should_preempt_barrier` is not unified-pool aware. | Medium | Found in the 2026-06-12 deep audit (fan-out subsystem). Three related termination backstops, all in the territory of RCA item 4 ("pool-independent preemption timer", deferred from the clone-termination plan, now folded into parallelism.md §Clone termination): (a) preemption writes the graceful-stop file but never escalates to SIGTERM/SIGKILL, so a clone wedged inside a provider call can hold the barrier toward the 10h cap despite preemption having fired; (b) clones are `start_new_session=True`, so root SIGKILL orphans them until organic exhaustion (now far more reliable thanks to the `[[BRANCH_COMPLETE]]` signal + relative low-output backstop); (c) the preemption capacity gate reads only the current provider's pool, so unified one-account-per-provider deployments never preempt. Deferring as one coherent work item rather than three point patches — the right shape is a single clone-side liveness/termination policy (parent-PID watch + bounded escalation), not more parent-side timers. | Deferred (RCA item 4 cluster) |
| Rate-limit detection layer 1 substring-scans stderr+stdout for bare tokens like `429`, so agent-authored text or incidental numbers combined with a non-zero exit can be misclassified as a rate limit (triggering rotation + session clear instead of the failure-streak path). | Medium | Found in the 2026-06-12 audit. Real but low-frequency: it requires a non-zero exit AND a matching substring in content. The detection heuristics are live-validated across three providers; tightening them (word-boundary anchoring, stderr-only scope for bare numerics) risks reintroducing missed rate-limits, which are far more costly (burned cycles on a capped account) than the rare false rotation. Revisit with transcript fixtures from a real misclassification rather than speculatively. | Deferred |
| Codex envelope parsing returns `is_error` on the first `error` event in the JSONL stream, discarding a turn that subsequently retried and completed successfully. | Low | Found in the 2026-06-12 audit. Needs a fixture of a real transient-then-recovered Codex stream to fix confidently; the conservative current behavior (treat as failure, retry next cycle) loses one turn but never corrupts state. The complementary silent-empty case (turn completed but `-o` output file missing) WAS fixed in the sweep — it now surfaces as an error instead of a fake successful empty turn. | Deferred |
| `long-exposure launch "<directive>"` resumes existing state with a task redirect, while `long-exposure start "<directive>"` archives state and starts fresh — two launch surfaces with different stale-state semantics. | Low | The resume-with-redirect path is intentional (guidance continuity for a directive pivot mid-campaign), but an agent following the cli-install adapter text on a machine with months-old default-instance state can silently resume a dead campaign. The wrapper's new liveness guard (2026-06-12) closes the worst variant (bare directive beside a live run). Full alignment needs a design decision: either `launch` archives like `start`, or the redirect notice becomes a confirmation gate. Until then the wrapper docs spell out the distinction. | Deferred (needs design decision) |
| `parse_outputs` accepts a named `[END OUTPUT: <name>]` close tag without verifying the name matches the opening `[OUTPUT: <name>]`, so a block that *quotes* another block's close marker is truncated at the quote. | Low | Found in the 2026-06-12 audit. Strict open/close name matching (backreference) could regress the lenient acceptance that the 2026-05-28 report-capture fix deliberately introduced (agents emit close-name variants like spacing/case drift). The longest-block-wins rule plus current-turn transcript recovery already bound the damage. Revisit only if a real run shows a truncated deliverable from an embedded marker quote. | Deferred |
| `long-exposure launch --manager` runs the manager poller as a thread inside the conductor process, sharing telemetry module globals and (on ACT/ESCALATE rotation) `os.environ` account pins with the main loop. | Low | The manager was designed as a cron *process* sidecar, and that remains the documented production mode. The threaded convenience mode is best-effort; a mid-cycle env repoint from the manager thread is theoretically possible but requires an ACT/ESCALATE verdict plus a rotation inside the poll window. Document-only for now; if threaded mode graduates beyond convenience, the fix is delegating manager agent calls to a subprocess. | Deferred |
| `telemetry.py` falls back to a cwd-relative events dir when neither `output_dir` nor `instance_dir` is configured, scattering events by invocation directory. | Low | Found in the 2026-06-12 audit. Affects only ad-hoc invocations outside a configured instance; normal runs always pass an instance dir. Graceful (never raises), just lossy for analysis. Align with `_user_writable_data_dir()` opportunistically. | Deferred |
| `peak_cycle_output` is a one-way ratchet: one anomalously verbose cycle permanently raises the relative low-output threshold (5% of peak) for the rest of the run. | Low | Anticipated in the clone-termination plan ("a min() ceiling could be added later if a real run ever false-closes") and accepted there; the 2026-06-12 audit re-confirmed the exposure but observed data still separates idle (~2%) from substantive (~7-13%) cleanly, `CLOSURE_COUNT=2` self-corrects single false-lows, and the calibration now survives stop/resume (persisted in state). Add a capped peak or recent-median statistic only on evidence of a real false closure. | Deferred (by plan) |
| `parse_outputs` longest-block-wins inverts on corrections: an agent that re-emits a *shorter corrected* block of the same name loses to its retracted longer draft; an auditor whose `[[BRANCH_COMPLETE]]` sits only in the shorter duplicate has the signal dropped (post-parse winner is what the closure check scans). | Low | Found in the 2026-06-12 review sweep. Longest-wins was chosen against the observed incident (a trailing stub clobbering the real 29 KB report under last-write-wins); the corrected-re-emission scenario is the mirror image and has not been observed. The low-output backstop covers a missed completion signal. A robust discriminator ("the block the agent meant") needs ordering+intent signals that don't exist in the marker grammar; revisit only on a real occurrence. | Deferred |
| Curator hard-excludes for `reports/final/` and `audits/final/` are now enumerated scratch-file lists, so a *future* scratch artifact added by a later code change ships in curated packages until the list is extended. | Low | The enumeration is the deliberate inverse of the prior bug (directory-level excludes silently dropped the canonical deliverables — the worse failure). Shipping requires the curator manifest to list the file, which is itself agent-curated against the include-policy prompt. Mitigation when touching the final-stage writers: add new scratch basenames to `_PACKAGE_HARD_EXCLUDE_PATTERNS` in the same change. | Accepted trade-off |
| If root pool-slot acquisition fails at startup (PoolExhausted/IO error → "continuing pinned without ledger entry"), `fanout_cap()` no longer reserves headroom for the slotless root, so a same-run fan-out can hand every slot to clones and oversubscribe the root's account. | Low | Found in the 2026-06-12 review sweep. Requires a startup ledger failure AND a fan-out in the same degraded run; the prior blanket `-1` also mis-stated capacity in the normal case (docs and unified path agree the root's held slot is the reserve). The right fix is making the degraded state explicit (root retries slot acquisition, or fanout_cap subtracts 1 only when the root holds no slot) — worth doing when the pool ledger next gets touched. | Deferred |
| Interactive transport duplicates small infrastructure: newline JSON-RPC framing + dispatch (vs `mcp_search_server.py`), pid-liveness (vs `pool._pid_alive`), a third atomic-write helper, and the driver contract prose (seed prompt vs tool description). | Low | Found in the 2026-06-12 review sweep. Consolidation is blocked partly by import direction (`exploration` imports `interactive_transport`, so the transport can't import exploration's helpers at module level) and partly by the bridge needing to run as a bare script. Worth a small shared `mcp_stdio.py`/util module if the interactive feature graduates from PoC; premature while it is compliance-gated and may be rearchitected. | Deferred (PoC) |
